(library (name uuid_unix_test)
 (libraries core expect_test_helpers_core uuid_unix)
 (preprocessor_deps
  (env_var TESTING_FRAMEWORK))
 (preprocess (pps ppx_jane))
 (inline_tests))

(env
 (_
  (env-vars
   (TESTING_FRAMEWORK inline-test))))
